// Bootstrap stub — boneyard's vite plugin overwrites this file after the
// first successful skeleton capture (dev server). Committed so the app
// (and the Vercel production build) can resolve the import even before any
// bones have been captured.
import { registerBones } from 'boneyard-js/react';

registerBones({});
